<form class="row g-3 searchalign" action="/" method="GET">
    <div class="col-auto">
        <input type="text" class="form-control searchfix" id="searchbar" name="brand" placeholder="Pesquisar">
    </div>
    <div class="col-auto">
        <button type="submit" class="btn btn-dark mb-3 btnfixsearch">🔍</button>
    </div>
</form>

<div class="info">
    <div class="block">
        <div class="input-field">
            <label for="number1">Total de objetos no inventário:{{totalItems}}</label>
        </div>
    </div>
</div>

<h2>itens pesquisados</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Código:</th>
            <th scope="col">Nome:</th>
            <th scope="col">Ações:</th>
        </tr>
    </thead>
    <tbody>
        {{#each searchResults}}
        <tr>
            <th scope="row">{{@index}}</th>
            <td class="title-columna"><a href="itens/{{this._id}}">{{ this.brand }}</a></td>
            <td class="title-columnb"><a href="itens/{{this._id}}">{{ this.objectname }}</a></td>
            <td class="actions-column">
                <a href="itens/{{this._id}}" class="btn btn-dark btnfix2">✏️</a>
                <form action="itens/delete" method="POST">
                    <input type ="hidden" name="id" value="{{this._id}}">
                    <input type="submit" class="btn btn-dark btnfix3" value="❌">
                </form>
            </td>
        </tr>
        {{else}}
            <p>Não há pesquisa! Favor pesquisar.</p>
        {{/each}}
    </tbody>
</table>
